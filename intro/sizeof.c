#include <stdio.h>
int main(void){
int a; double b; char c;
printf("size of a in memory is:%zu\n", sizeof(a));
printf("size of b in memory is:%zu\n", sizeof(b));
printf("size of c in memory is:%zu\n", sizeof(c));
return 0;
}
